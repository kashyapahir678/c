#include <stdio.h>
#include <conio.h>

void main() {

   int p,r,t;
   int total=0;
   clrscr();
   printf("enter the value of p");
   scanf("%d",&p);

   printf("enter the value of r");
   scanf("%d",&r);

   printf("enter the value of t");
   scanf("%d",&t);

   total=p*r*t/100;

   printf("PRT value is %d",total);
}